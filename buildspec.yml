version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - echo "Build started"

artifacts:
  files:
    - app.py
    - requirements.txt
  discard-paths: yes
